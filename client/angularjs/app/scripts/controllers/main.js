'use strict';

angular.module('scrumTimeApp')
  .controller('MainCtrl', function ($scope) {
    $scope.awesomeThings = [
      'Stories that can inherit from stories',
      'Tasks',
	  'Etc.'
    ];
  });
